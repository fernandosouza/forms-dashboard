{namespace FormsDashboard}

/**
 * This renders the main element.
 * @param? elementClasses
 */
{template .render}
	<div class="formsDashboard{$elementClasses ? ' ' + $elementClasses : ''}">
	</div>
{/template}
